//tipagem dinâmica
let minhaVar = 300
minhaVar = "trezentos"
minhaVar = true;

console.log(minhaVar) //retornou true

//tipagem dinâmica significa que não precisamos declarar o tipo dessa variável, o js já entende que o tipo está sendo mudado.